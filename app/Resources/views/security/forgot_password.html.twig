{% extends ':home:index.html.twig' %}

{#{% block javascripts %}#}
    {#{{ parent() }}#}
{#{% endblock %}#}


{% block body %}
    <div class="return col-xs-1 col-sm-3 col-md-2">
        <a onclick="javascript:history.back(); return false;">
            <i class="glyphicon glyphicon-chevron-left"></i>
            <span class="hidden-xs">Вернуться к профилю</span>
        </a>
    </div>
    <div style="margin: 2%" class="col-xs-12 col-md-offset-1 col-md-4">
        <h2>Редактирование профиля</h2>
        {{ form_start(form, {
            'action': path('reset_password'),
            'method': 'POST' }) }}
        {{ form_widget(form) }}
        <input type="submit" value="Сохранить" id="saveCategory" class="col-xs-12 col-md-3 btn btn-default" />
        {{ form_end(form) }}
        <div class="list-group"></div>
    </div>
{% endblock %}