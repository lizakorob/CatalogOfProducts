{% extends "base.html.twig" %}

{% block body %}
    {% for flash_message in app.session.flashbag.get('notice') %}
        {{ flash_message }}
    {% endfor %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}